export enum RoleEnum {
  ADMIN = 'admin',
  USER = 'user',
  MANAGER = 'manager',
}

export enum TokenTypeEnum {
  ACCESS = 'access',
  REFRESH = 'refresh',
  CONFIRMATION = 'confirmation',
  RESET_PASSWORD = 'resetPassword',
}
